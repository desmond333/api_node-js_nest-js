const password = '123';
const user = 'Alex';
const name = 'pictures';
// myFirstDatabase

export const dbUrl = `mongodb+srv://${user}:${password}@cluster0.6rfle.mongodb.net/${name}?retryWrites=true&w=majority`;
